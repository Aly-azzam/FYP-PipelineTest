# ==========================================
# SAM VIDEO COMPARE DEMO - requirements.txt
# ==========================================
# IMPORTANT
# 1) Créez un nouvel environnement virtuel
# 2) Installez PyTorch d'abord
# 3) Ensuite: pip install -r requirements.txt
#
# CPU:
#   pip install torch torchvision
#
# NVIDIA GPU (exemple CUDA 12.1):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#
# Si votre ami a une autre version CUDA, il change seulement cette commande PyTorch.
# ==========================================

# Core
numpy>=1.26,<2.0
opencv-python>=4.10,<5.0
Pillow>=10.0,<12.0
gradio>=5.0,<6.0

# SAM2 / config
hydra-core>=1.3,<1.4
iopath>=0.1.10,<0.2
PyYAML>=6.0,<7.0
tqdm>=4.66,<5.0
ninja>=1.11,<2.0
packaging>=24.0,<25.0

# Video export
imageio-ffmpeg>=0.6.0,<0.7

# Benchmark / analysis
psutil>=5.9,<8.0
pandas>=2.2,<3.0
matplotlib>=3.8,<4.0
nvidia-ml-py3>=7.352.0,<8.0

# Build helpers
setuptools>=68.0
wheel>=0.43

# Install local SAM2 package from repo
-e ./sam2_repo